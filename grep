The command grep looks for words inside files. 
sintax is: grep destination cp
##this command will provide all the sentences within the cp file where the word destination is present
it can be combined with multiple commands, e.g. ps | grep pablo #it will list all the processes where the user is pablo
